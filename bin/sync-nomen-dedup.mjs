#!/usr/bin/env node
/**
 * ОТКЛЮЧЕНО: дедупы/мастера из Google Sheet больше не используются.
 * Номенклатура — только из синка 1С HS → WMS.
 */
console.error('sync-nomen-dedup: отключено. Номенклатура из 1С, дедупы не применяются.');
process.exit(1);
