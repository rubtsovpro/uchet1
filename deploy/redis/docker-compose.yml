# Local Postgres (опционально) + Redis для hot-cache WMS
services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    command: ['redis-server', '--save', '', '--appendonly', 'no']
    restart: unless-stopped
